/*function used to display and terminate after the time limit
setTimeout(()=>{
    console.log("time limit exceeded(3sec completed)");
},3000);*/

/*function used to create an interval and also to display the content after evry interval
var time=0;
var timer=setInterval(()=>{
    time+=2;
    console.log(time+" seconds have passed");
    if(time>=10){
    clearInterval(timer);
}},2000);*/

//directory display
//console.log(__dirname);
//display filename
console.log(__filename);